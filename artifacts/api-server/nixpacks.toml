[phases.install]
cmds = ['npm install -g pnpm@10.26.1', 'export PATH="/root/.npm-global/bin:$PATH" && pnpm install --frozen-lockfile']

[phases.build]
cmds = ['export PATH="/root/.npm-global/bin:$PATH" && pnpm run build']

[start]
cmd = 'node dist/index.js'
